<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - Tax Regime</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="styles/login.css" />
  </head>
  <body>
    <!-- navbar  -->
    <nav class="navbar navbar-dark bg-dark fixed-top">
      <div class="container-fluid">
        <button
          class="navbar-toggler me-2 text-white"
          type="button"
          data-bs-toggle="offcanvas"
          data-bs-target="#offcanvasDarkNavbar"
          aria-controls="offcanvasDarkNavbar"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <a class="navbar-brand me-auto" href="#" style="padding-left: 10px"
          >Tax Regime</a
        >
        <a href="/register" class="btn btn-link login-link">Sign Up</a>

        <!-- Offcanvas content for mobile view -->
        <div
          class="offcanvas offcanvas-start text-bg-dark"
          tabindex="-1"
          id="offcanvasDarkNavbar"
          aria-labelledby="offcanvasDarkNavbarLabel"
        >
          <div class="offcanvas-header">
            <h5
              class="offcanvas-title"
              style="padding-top: 15px"
              id="offcanvasDarkNavbarLabel"
            >
              Tax Regime
            </h5>
            <button
              type="button"
              class="btn-close btn-close-white"
              data-bs-dismiss="offcanvas"
              aria-label="Close"
            ></button>
          </div>

          <hr class="bg-white" style="height: 2px" />

          <div class="offcanvas-body">
            <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="#">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" href="#">Change Password</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" href="/register">Registeration</a>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </nav>

    <div
      class="container d-flex justify-content-center align-items-center min-vh-100"
    >
      <div class="card p-4 shadow-lg">
        <h2 class="text-center mb-4">Log In</h2>
        <p class="text-center">
          Please enter your userid and password of RFID system.
        </p>
        <% if (error) { %>
        <div class="alert-danger text-center"><%= error %></div>
        <% } %>
        <form id="loginForm" action="/submit" method="post">
          <fieldset class="border p-3 mb-3">
            <legend class="float-none w-auto px-2 text-primary">
              User Information
            </legend>
            <div class="mb-3">
              <label for="aadharno" class="form-label">Aadhar No.</label>
              <input
                type="text"
                class="form-control"
                id="aadharno"
                name="aadharno"
                placeholder="Enter your Aadhar No."
              />
            </div>
            <div class="mb-3">
              <label for="password" class="form-label">Password</label>
              <input
                type="password"
                class="form-control"
                id="password"
                name="password"
                placeholder="Enter your password"
              />
            </div>
          </fieldset>
          <div class="form-check mb-3">
            <input
              class="form-check-input"
              type="checkbox"
              id="keepLoggedIn"
              name="keepLoggedIn"
            />
            <label class="form-check-label" for="keepLoggedIn">
              Keep me logged in
            </label>
          </div>
          <button type="submit" class="btn btn-success w-100">Log In</button>
          <div class="d-flex justify-content-between mt-3">
            <a href="#" class="text-decoration-none hyperlinks"
              >Forgot Password?</a
            >
            <a href="/register" class="text-decoration-none hyperlinks"
              >Sign Up</a
            >
          </div>
        </form>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
